// fibers Theme Configuration

import type { ThemeConfig } from '../types';

export const config: ThemeConfig = {
  id: 'fibers',
  name: 'Fibers',
  description: 'Tennis ball felt - volumetric fiber attenuation',
  previewColor: '#d9f026',
  isDark: true,
};
