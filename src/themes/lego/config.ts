// Lego Theme Configuration

import type { ThemeConfig } from '../types';

export const config: ThemeConfig = {
  id: 'lego',
  name: 'Lego',
  description: 'Tiled Lego brick surface with studs',
  previewColor: '#4f94cd',
  isDark: true,
};
