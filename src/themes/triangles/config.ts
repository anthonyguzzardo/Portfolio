// Triangles Theme Configuration

import type { ThemeConfig } from '../types';

export const config: ThemeConfig = {
  id: 'triangles',
  name: 'Magma',
  description: 'Charcoal and lava triangular mesh',
  previewColor: '#ff6600',
  isDark: true,
};
