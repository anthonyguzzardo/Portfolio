---
const CONTACT_EMAIL = 'shakshuka.photos@gmail.com';

const galleryItems = [
  { id: 1, type: 'photo', src: 'https://picsum.photos/seed/hawk1/1200/800', thumb: 'https://picsum.photos/seed/hawk1/600/400', title: 'Red-tailed Hawk in Flight', species: 'Buteo jamaicensis', category: 'birds', location: 'Starved Rock State Park, IL', date: '2025-11-15', forSale: true },
  { id: 2, type: 'photo', src: 'https://picsum.photos/seed/heron2/1200/800', thumb: 'https://picsum.photos/seed/heron2/600/400', title: 'Great Blue Heron at Dawn', species: 'Ardea herodias', category: 'birds', location: "Chain O' Lakes, IL", date: '2025-10-22', forSale: true },
  { id: 3, type: 'photo', src: 'https://picsum.photos/seed/fox3/1200/800', thumb: 'https://picsum.photos/seed/fox3/600/400', title: 'Red Fox in Morning Mist', species: 'Vulpes vulpes', category: 'mammals', location: 'Yellowstone NP, WY', date: '2025-09-08', forSale: true },
  { id: 4, type: 'photo', src: 'https://picsum.photos/seed/eagle4/1200/800', thumb: 'https://picsum.photos/seed/eagle4/600/400', title: 'Bald Eagle Perched', species: 'Haliaeetus leucocephalus', category: 'birds', location: 'Mississippi River Valley, IA', date: '2025-12-03', forSale: true },
  { id: 5, type: 'photo', src: 'https://picsum.photos/seed/deer5/1200/800', thumb: 'https://picsum.photos/seed/deer5/600/400', title: 'White-tailed Deer at Dusk', species: 'Odocoileus virginianus', category: 'mammals', location: 'Shawnee National Forest, IL', date: '2025-08-19', forSale: false },
  { id: 6, type: 'photo', src: 'https://picsum.photos/seed/owl6/1200/800', thumb: 'https://picsum.photos/seed/owl6/600/400', title: 'Great Horned Owl', species: 'Bubo virginianus', category: 'birds', location: 'Morton Arboretum, IL', date: '2026-01-12', forSale: true },
  { id: 7, type: 'video', src: '', thumb: 'https://picsum.photos/seed/kingfisher7/600/400', title: 'Belted Kingfisher Diving', species: 'Megaceryle alcyon', category: 'birds', location: 'Galena, IL', date: '2025-07-04', forSale: false },
  { id: 8, type: 'photo', src: 'https://picsum.photos/seed/landscape8/1200/800', thumb: 'https://picsum.photos/seed/landscape8/600/400', title: 'Sunrise Over the Wetlands', species: '', category: 'landscapes', location: 'Horicon Marsh, WI', date: '2025-06-21', forSale: true },
  { id: 9, type: 'photo', src: 'https://picsum.photos/seed/warbler9/1200/800', thumb: 'https://picsum.photos/seed/warbler9/600/400', title: 'Yellow Warbler on Branch', species: 'Setophaga petechia', category: 'birds', location: 'Magee Marsh, OH', date: '2025-05-10', forSale: true },
  { id: 10, type: 'photo', src: 'https://picsum.photos/seed/turtle10/1200/800', thumb: 'https://picsum.photos/seed/turtle10/600/400', title: 'Painted Turtle Sunbathing', species: 'Chrysemys picta', category: 'reptiles', location: 'Indiana Dunes, IN', date: '2025-07-28', forSale: false },
  { id: 11, type: 'photo', src: 'https://picsum.photos/seed/sandhill11/1200/800', thumb: 'https://picsum.photos/seed/sandhill11/600/400', title: 'Sandhill Cranes Migration', species: 'Antigone canadensis', category: 'birds', location: 'Jasper-Pulaski FWA, IN', date: '2025-11-01', forSale: true },
  { id: 12, type: 'photo', src: 'https://picsum.photos/seed/coyote12/1200/800', thumb: 'https://picsum.photos/seed/coyote12/600/400', title: 'Coyote in Snow', species: 'Canis latrans', category: 'mammals', location: 'Grand Teton NP, WY', date: '2026-02-14', forSale: true },
];

const stats = [
  { label: 'Species Identified', value: 247 },
  { label: 'Photos Taken', value: 12480 },
  { label: 'States Visited', value: 5 },
  { label: 'Hours in Field', value: 1860 },
  { label: 'Life List Species', value: 312 },
  { label: 'Sightings This Month', value: 14 },
];

const recentSightings = [
  { species: 'Pileated Woodpecker', location: 'Starved Rock, IL', date: '2026-03-28' },
  { species: 'American Kestrel', location: 'Midewin Tallgrass Prairie, IL', date: '2026-03-25' },
  { species: 'Snowy Owl', location: 'Illinois Beach State Park, IL', date: '2026-03-20' },
  { species: 'Northern Cardinal', location: 'Morton Arboretum, IL', date: '2026-03-18' },
  { species: "Cooper's Hawk", location: 'Waterfall Glen, IL', date: '2026-03-15' },
];

const stateVisits: Record<string, { name: string; visits: number; species: number; photos: number; topSpecies: string }> = {
  IL: { name: 'Illinois', visits: 45, species: 87, photos: 3200, topSpecies: 'Red-tailed Hawk' },
  WI: { name: 'Wisconsin', visits: 22, species: 54, photos: 1800, topSpecies: 'Sandhill Crane' },
  IN: { name: 'Indiana', visits: 18, species: 42, photos: 1200, topSpecies: 'Eastern Bluebird' },
  MI: { name: 'Michigan', visits: 15, species: 38, photos: 980, topSpecies: 'Common Loon' },
  OH: { name: 'Ohio', visits: 12, species: 45, photos: 850, topSpecies: 'Yellow Warbler' },
};

const fieldGuide = [
  { id: 1, name: 'Red-tailed Hawk', scientific: 'Buteo jamaicensis', img: 'https://picsum.photos/seed/rthawk/400/500', habitat: 'Open fields, forests', diet: 'Small mammals, birds', wingspan: '45-52 in', weight: '1.5-3.5 lb', status: 'Least Concern', firstSeen: '2024-03-12', location: 'Starved Rock, IL', sightings: 23, rarity: 'common' },
  { id: 2, name: 'Great Blue Heron', scientific: 'Ardea herodias', img: 'https://picsum.photos/seed/gbheron/400/500', habitat: 'Wetlands, shorelines', diet: 'Fish, amphibians', wingspan: '66-79 in', weight: '4.6-5.5 lb', status: 'Least Concern', firstSeen: '2024-04-08', location: "Chain O' Lakes, IL", sightings: 18, rarity: 'common' },
  { id: 3, name: 'Bald Eagle', scientific: 'Haliaeetus leucocephalus', img: 'https://picsum.photos/seed/baldeagle/400/500', habitat: 'Near large bodies of water', diet: 'Fish, waterfowl', wingspan: '71-91 in', weight: '6.6-14 lb', status: 'Least Concern', firstSeen: '2024-06-20', location: 'Mississippi River Valley, IA', sightings: 8, rarity: 'uncommon' },
  { id: 4, name: 'Great Horned Owl', scientific: 'Bubo virginianus', img: 'https://picsum.photos/seed/ghowl/400/500', habitat: 'Forests, swamps, deserts', diet: 'Mammals, birds, reptiles', wingspan: '40-57 in', weight: '2-5.5 lb', status: 'Least Concern', firstSeen: '2025-01-12', location: 'Morton Arboretum, IL', sightings: 5, rarity: 'uncommon' },
  { id: 5, name: 'Belted Kingfisher', scientific: 'Megaceryle alcyon', img: 'https://picsum.photos/seed/bkingfish/400/500', habitat: 'Streams, rivers, ponds', diet: 'Fish, crayfish', wingspan: '19-23 in', weight: '5-6 oz', status: 'Least Concern', firstSeen: '2025-07-04', location: 'Galena, IL', sightings: 3, rarity: 'uncommon' },
  { id: 6, name: 'Yellow Warbler', scientific: 'Setophaga petechia', img: 'https://picsum.photos/seed/ywarbler/400/500', habitat: 'Thickets, wetlands', diet: 'Insects, berries', wingspan: '6.3-7.9 in', weight: '0.3-0.4 oz', status: 'Least Concern', firstSeen: '2025-05-10', location: 'Magee Marsh, OH', sightings: 12, rarity: 'common' },
  { id: 7, name: 'Sandhill Crane', scientific: 'Antigone canadensis', img: 'https://picsum.photos/seed/shcrane/400/500', habitat: 'Marshes, grasslands', diet: 'Seeds, grains, insects', wingspan: '73-90 in', weight: '7.3-11 lb', status: 'Least Concern', firstSeen: '2025-11-01', location: 'Jasper-Pulaski FWA, IN', sightings: 15, rarity: 'common' },
  { id: 8, name: 'Snowy Owl', scientific: 'Bubo scandiacus', img: 'https://picsum.photos/seed/snowyowl/400/500', habitat: 'Open tundra, fields', diet: 'Lemmings, small mammals', wingspan: '49-59 in', weight: '3.5-6.5 lb', status: 'Vulnerable', firstSeen: '2026-03-20', location: 'Illinois Beach SP, IL', sightings: 1, rarity: 'rare' },
  { id: 9, name: 'Pileated Woodpecker', scientific: 'Dryocopus pileatus', img: 'https://picsum.photos/seed/pwood/400/500', habitat: 'Mature forests', diet: 'Carpenter ants, beetles', wingspan: '26-30 in', weight: '8.8-12.3 oz', status: 'Least Concern', firstSeen: '2026-03-28', location: 'Starved Rock, IL', sightings: 4, rarity: 'uncommon' },
  { id: 10, name: 'American Kestrel', scientific: 'Falco sparverius', img: 'https://picsum.photos/seed/akestrel/400/500', habitat: 'Open country, cities', diet: 'Insects, small birds', wingspan: '20-24 in', weight: '2.8-5.8 oz', status: 'Least Concern', firstSeen: '2026-03-25', location: 'Midewin Prairie, IL', sightings: 6, rarity: 'common' },
  { id: 11, name: 'Northern Cardinal', scientific: 'Cardinalis cardinalis', img: 'https://picsum.photos/seed/ncard/400/500', habitat: 'Woodlands, gardens', diet: 'Seeds, fruits, insects', wingspan: '10-12 in', weight: '1.5-1.7 oz', status: 'Least Concern', firstSeen: '2024-02-14', location: 'Morton Arboretum, IL', sightings: 47, rarity: 'common' },
  { id: 12, name: "Cooper's Hawk", scientific: 'Accipiter cooperii', img: 'https://picsum.photos/seed/coophawk/400/500', habitat: 'Forests, wooded suburbs', diet: 'Medium-sized birds', wingspan: '24-36 in', weight: '7.8-24 oz', status: 'Least Concern', firstSeen: '2026-03-15', location: 'Waterfall Glen, IL', sightings: 7, rarity: 'uncommon' },
  { id: 13, name: 'Common Loon', scientific: 'Gavia immer', img: 'https://picsum.photos/seed/cloon/400/500', habitat: 'Lakes, coastal waters', diet: 'Fish', wingspan: '41-52 in', weight: '6.5-12 lb', status: 'Least Concern', firstSeen: '2025-08-15', location: 'Traverse City, MI', sightings: 3, rarity: 'uncommon' },
  { id: 14, name: 'Eastern Bluebird', scientific: 'Sialia sialis', img: 'https://picsum.photos/seed/eblue/400/500', habitat: 'Open woodlands, farmland', diet: 'Insects, berries', wingspan: '9.8-12.6 in', weight: '0.95-1.2 oz', status: 'Least Concern', firstSeen: '2025-04-22', location: 'Brown County SP, IN', sightings: 9, rarity: 'common' },
];

const categories = ['all', 'birds', 'mammals', 'reptiles', 'landscapes'];

const iconPaths = [
  '<path d="M20.24 12.24a6 6 0 0 0-8.49-8.49L5 10.5V19h8.5z"/><line x1="16" y1="8" x2="2" y2="22"/>',
  '<path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/>',
  '<path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/>',
  '<circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>',
  '<path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2"/><rect x="8" y="2" width="8" height="4" rx="1"/>',
  '<path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/>',
];
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shakshuka | Wildlife Photography</title>
  <meta name="description" content="Wildlife and bird photography by Shakshuka. Capturing nature's finest moments across North America.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
</head>
<body>

  <!-- NAV -->
  <nav class="sk-nav" id="sk-nav">
    <div class="sk-nav-inner">
      <a href="#" class="sk-nav-logo">Shakshuka</a>
      <button class="sk-nav-toggle" id="sk-nav-toggle" aria-label="Toggle navigation">
        <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2">
          <line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/>
        </svg>
      </button>
      <ul class="sk-nav-links" id="sk-nav-links">
        <li><a href="#gallery">Gallery</a></li>
        <li><a href="#field-guide">Field Guide</a></li>
        <li><a href="#stats">Stats</a></li>
        <li><a href="#map">Map</a></li>
        <li><a href={`mailto:${CONTACT_EMAIL}`}>Contact</a></li>
      </ul>
    </div>
  </nav>

  <!-- HERO -->
  <section class="sk-hero">
    <div class="sk-hero-bg"></div>
    <div class="sk-hero-content">
      <p class="sk-hero-label">Wildlife & Bird Photography</p>
      <h1>Shakshuka</h1>
      <p class="sk-hero-subtitle">Through the Lens of the Wild</p>
      <p class="sk-hero-bio">
        Amateur birder and wildlife photographer documenting the beauty of North American
        birds and animals. Every frame tells a story of patience, wonder, and the quiet
        magic of the natural world.
      </p>
      <div class="sk-hero-buttons">
        <a href="#gallery" class="sk-btn-primary">Explore Gallery</a>
        <a href={`mailto:${CONTACT_EMAIL}`} class="sk-btn-outline">Get in Touch</a>
      </div>
    </div>
  </section>

  <!-- GALLERY -->
  <section id="gallery" class="sk-section">
    <div class="sk-container">
      <h2 class="sk-section-title sk-anim">Through the Lens</h2>
      <p class="sk-section-sub sk-anim">A curated collection of wildlife encounters</p>
      <div class="sk-filters sk-anim">
        {categories.map((cat, i) => (
          <button class={`sk-filter-btn${i === 0 ? ' active' : ''}`} data-filter={cat}>{cat}</button>
        ))}
      </div>
      <div class="sk-gallery-grid">
        {galleryItems.map(item => (
          <div class="sk-card sk-anim" data-category={item.category} data-id={item.id}>
            <img src={item.thumb} alt={item.title} loading="lazy" />
            {item.type === 'video' && (
              <div class="sk-play-icon">
                <svg viewBox="0 0 24 24" width="24" height="24" fill="white"><polygon points="8,5 19,12 8,19"/></svg>
              </div>
            )}
            {item.forSale && <span class="sk-badge">For Sale</span>}
            <div class="sk-card-overlay">
              <div class="sk-card-title">{item.title}</div>
              <div class="sk-card-loc">{item.location}</div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- FIELD GUIDE -->
  <section id="field-guide" class="sk-section">
    <div class="sk-container">
      <h2 class="sk-section-title sk-anim">Field Guide</h2>
      <p class="sk-section-sub sk-anim">Flip through the life list — {fieldGuide.length} species catalogued</p>

      <div class="sk-dex">
        <button class="sk-dex-nav sk-dex-prev" id="dex-prev" aria-label="Previous">
          <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>
        </button>

        <div class="sk-dex-viewport" id="dex-viewport">
          {fieldGuide.map((bird, i) => (
            <div class="sk-dex-card" data-index={i} data-rarity={bird.rarity}>
              <div class="sk-dex-card-inner">
                <div class="sk-dex-front">
                  <div class="sk-dex-number">#{String(bird.id).padStart(3, '0')}</div>
                  <div class="sk-dex-rarity">{bird.rarity}</div>
                  <div class="sk-dex-img-wrap">
                    <img src={bird.img} alt={bird.name} loading="lazy" />
                  </div>
                  <h3 class="sk-dex-name">{bird.name}</h3>
                  <p class="sk-dex-scientific">{bird.scientific}</p>
                  <div class="sk-dex-quick">
                    <span>{bird.wingspan}</span>
                    <span>{bird.sightings} sightings</span>
                  </div>
                  <p class="sk-dex-hint">Click to flip</p>
                </div>
                <div class="sk-dex-back">
                  <div class="sk-dex-number">#{String(bird.id).padStart(3, '0')}</div>
                  <h3 class="sk-dex-name">{bird.name}</h3>
                  <div class="sk-dex-details">
                    <div class="sk-dex-row"><span class="sk-dex-label">Habitat</span><span>{bird.habitat}</span></div>
                    <div class="sk-dex-row"><span class="sk-dex-label">Diet</span><span>{bird.diet}</span></div>
                    <div class="sk-dex-row"><span class="sk-dex-label">Wingspan</span><span>{bird.wingspan}</span></div>
                    <div class="sk-dex-row"><span class="sk-dex-label">Weight</span><span>{bird.weight}</span></div>
                    <div class="sk-dex-row"><span class="sk-dex-label">Status</span><span class={bird.status === 'Vulnerable' ? 'sk-dex-warn' : ''}>{bird.status}</span></div>
                    <div class="sk-dex-row"><span class="sk-dex-label">First Seen</span><span>{new Date(bird.firstSeen + 'T00:00:00').toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}</span></div>
                    <div class="sk-dex-row"><span class="sk-dex-label">Location</span><span>{bird.location}</span></div>
                    <div class="sk-dex-row"><span class="sk-dex-label">Sightings</span><span>{bird.sightings}</span></div>
                  </div>
                  <p class="sk-dex-hint">Click to flip back</p>
                </div>
              </div>
            </div>
          ))}
        </div>

        <button class="sk-dex-nav sk-dex-next" id="dex-next" aria-label="Next">
          <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
        </button>
      </div>

      <div class="sk-dex-counter sk-anim">
        <span id="dex-current">1</span> / <span id="dex-total">{fieldGuide.length}</span>
      </div>
    </div>
  </section>

  <!-- STATS -->
  <section id="stats" class="sk-section">
    <div class="sk-container">
      <h2 class="sk-section-title sk-anim">By the Numbers</h2>
      <p class="sk-section-sub sk-anim">A birder's journey in data</p>
      <div class="sk-stats-grid">
        {stats.map((stat, i) => (
          <div class="sk-stat-card sk-anim">
            <div class="sk-stat-icon">
              <svg viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="1.5">
                <Fragment set:html={iconPaths[i]} />
              </svg>
            </div>
            <div class="sk-stat-num" data-target={stat.value}>0</div>
            <div class="sk-stat-label">{stat.label}</div>
          </div>
        ))}
      </div>
      <div class="sk-recent sk-anim">
        <h3>Recent Sightings</h3>
        {recentSightings.map(s => (
          <div class="sk-sighting">
            <div>
              <div class="sk-sighting-species">{s.species}</div>
              <div class="sk-sighting-loc">{s.location}</div>
            </div>
            <div class="sk-sighting-date">
              {new Date(s.date + 'T00:00:00').toLocaleDateString('en-US', { month: 'short', day: 'numeric' })}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- MAP -->
  <section id="map" class="sk-section">
    <div class="sk-container">
      <h2 class="sk-section-title sk-anim">Where I've Been</h2>
      <p class="sk-section-sub sk-anim">5 states explored and counting</p>

      <div class="sk-map-chips sk-anim">
        <button class="sk-chip active" data-metric="visits">Visits</button>
        <button class="sk-chip" data-metric="species">Species</button>
        <button class="sk-chip" data-metric="photos">Photos</button>
      </div>

      <div class="sk-map-wrap sk-anim">
        <div class="sk-svg-container" id="sk-svg-container">
          <div class="sk-map-loading">Loading map...</div>
        </div>
        <div class="sk-map-legend">
          <span class="sk-legend-min">0</span>
          <div class="sk-legend-gradient"></div>
          <span class="sk-legend-max">45</span>
        </div>
      </div>

      <div class="sk-state-detail" id="sk-state-detail" style="display:none">
        <button class="sk-detail-close" id="sk-detail-close" aria-label="Close detail">
          <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
          </svg>
        </button>
        <h3 class="sk-detail-name"></h3>
        <div class="sk-detail-grid">
          <div class="sk-detail-item"><span class="sk-detail-val" id="sd-visits">--</span><span class="sk-detail-label">Visits</span></div>
          <div class="sk-detail-item"><span class="sk-detail-val" id="sd-species">--</span><span class="sk-detail-label">Species</span></div>
          <div class="sk-detail-item"><span class="sk-detail-val" id="sd-photos">--</span><span class="sk-detail-label">Photos</span></div>
        </div>
        <p class="sk-detail-top">Top species: <strong class="sk-detail-top-name"></strong></p>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="sk-footer">
    <div class="sk-container">
      <p class="sk-footer-tag">Built with patience, long lenses, and early mornings.</p>
      <p class="sk-footer-copy">&copy; 2026 Shakshuka Photography. All rights reserved.</p>
    </div>
  </footer>

  <!-- LIGHTBOX -->
  <div class="sk-lightbox" id="sk-lightbox" role="dialog" aria-label="Image viewer">
    <button class="sk-lb-close" aria-label="Close">
      <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
    <button class="sk-lb-nav sk-lb-prev" aria-label="Previous">
      <svg viewBox="0 0 24 24" width="36" height="36" fill="none" stroke="currentColor" stroke-width="2">
        <polyline points="15 18 9 12 15 6"/>
      </svg>
    </button>
    <button class="sk-lb-nav sk-lb-next" aria-label="Next">
      <svg viewBox="0 0 24 24" width="36" height="36" fill="none" stroke="currentColor" stroke-width="2">
        <polyline points="9 18 15 12 9 6"/>
      </svg>
    </button>
    <div class="sk-lb-body">
      <img class="sk-lb-img" src="" alt="" />
      <div class="sk-lb-info">
        <h3 class="sk-lb-title"></h3>
        <p class="sk-lb-species"></p>
        <p class="sk-lb-meta"></p>
        <a class="sk-lb-buy" href="#">Inquire to Purchase</a>
      </div>
    </div>
  </div>

  <!-- MAP TOOLTIP -->
  <div class="sk-tooltip" id="sk-tooltip">
    <div class="sk-tooltip-name"></div>
    <div class="sk-tooltip-metrics"></div>
    <div class="sk-tooltip-species"></div>
  </div>

</body>
</html>

<style is:global>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  html { scroll-behavior: smooth; scroll-padding-top: 20px; }
  body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    background: #0f1210;
    color: #e8e4dc;
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
    overflow-x: hidden;
  }
  img { max-width: 100%; display: block; }
  a { color: inherit; text-decoration: none; }
  button { font: inherit; cursor: pointer; border: none; background: none; color: inherit; }
  h1, h2, h3 { font-family: 'Playfair Display', Georgia, serif; font-weight: 600; line-height: 1.2; }

  .sk-container { max-width: 1200px; margin: 0 auto; padding: 0 2rem; }
  .sk-section { padding: 6rem 0; }
  .sk-section-title { font-size: 2.5rem; margin-bottom: 0.5rem; text-align: center; }
  .sk-section-sub { color: #8a8474; font-size: 1.1rem; font-weight: 300; margin-bottom: 3rem; text-align: center; }

  /* NAV */
  .sk-nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    padding: 1.25rem 2rem; transition: background 0.3s, box-shadow 0.3s;
  }
  .sk-nav.scrolled { background: rgba(15,18,16,0.95); backdrop-filter: blur(12px); box-shadow: 0 1px 0 rgba(255,255,255,0.05); }
  .sk-nav-inner { max-width: 1200px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center; }
  .sk-nav-logo { font-family: 'Playfair Display', serif; font-size: 1.4rem; font-weight: 700; letter-spacing: 0.5px; }
  .sk-nav-links { display: flex; gap: 2.5rem; list-style: none; }
  .sk-nav-links a {
    font-size: 0.9rem; font-weight: 400; opacity: 0.7; text-transform: uppercase; letter-spacing: 1px;
    position: relative; padding-bottom: 4px;
    transition: opacity 0.3s, color 0.3s;
  }
  .sk-nav-links a::after {
    content: ''; position: absolute; bottom: 0; left: 50%; width: 0; height: 2px;
    background: #7DB356; transition: width 0.3s ease, left 0.3s ease;
  }
  .sk-nav-links a:hover { opacity: 1; color: #7DB356; }
  .sk-nav-links a:hover::after { width: 100%; left: 0; }
  .sk-nav-links a:active { transform: scale(0.95); }
  .sk-nav-toggle { display: none; }

  /* HERO */
  .sk-hero { min-height: 100vh; display: flex; align-items: center; justify-content: center; text-align: center; position: relative; overflow: hidden; }
  .sk-hero-bg {
    position: absolute; inset: 0;
    background: url('https://picsum.photos/seed/wildnature/1920/1080') center/cover no-repeat;
  }
  .sk-hero-bg::after {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(to bottom, rgba(15,18,16,0.82) 0%, rgba(15,18,16,0.88) 50%, rgba(15,18,16,0.98) 100%);
  }
  .sk-hero-content { position: relative; z-index: 1; max-width: 700px; padding: 2rem; }
  .sk-hero-label { text-transform: uppercase; letter-spacing: 5px; font-size: 0.85rem; color: #7DB356; margin-bottom: 1.5rem; font-weight: 500; }
  .sk-hero h1 { font-size: 5rem; margin-bottom: 0.75rem; font-weight: 700; letter-spacing: 1px; }
  .sk-hero-subtitle { font-size: 1.3rem; color: #a09882; margin-bottom: 1.5rem; font-family: 'Playfair Display', serif; font-style: italic; }
  .sk-hero-bio { font-size: 1rem; color: #8a8474; max-width: 520px; margin: 0 auto 2.5rem; line-height: 1.8; font-weight: 300; }
  .sk-hero-buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }

  .sk-btn-primary {
    padding: 0.85rem 2.25rem; background: #5B8C3E; color: white; border-radius: 4px;
    font-weight: 500; font-size: 0.9rem; letter-spacing: 0.5px; transition: background 0.2s, transform 0.2s;
  }
  .sk-btn-primary:hover { background: #4a7a35; transform: translateY(-1px); }
  .sk-btn-outline {
    padding: 0.85rem 2.25rem; border: 1px solid rgba(255,255,255,0.2); border-radius: 4px;
    font-weight: 500; font-size: 0.9rem; letter-spacing: 0.5px; transition: border-color 0.2s, color 0.2s;
  }
  .sk-btn-outline:hover { border-color: #5B8C3E; color: #7DB356; }

  /* GALLERY */
  .sk-filters { display: flex; gap: 0.75rem; justify-content: center; margin-bottom: 2.5rem; flex-wrap: wrap; }
  .sk-filter-btn {
    padding: 0.5rem 1.5rem; border-radius: 100px; border: 1px solid #2a302c;
    font-size: 0.85rem; text-transform: capitalize; transition: all 0.2s; background: transparent;
  }
  .sk-filter-btn.active, .sk-filter-btn:hover { background: #5B8C3E; border-color: #5B8C3E; color: white; }

  .sk-gallery-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 1.25rem; }
  .sk-card {
    position: relative; border-radius: 8px; overflow: hidden; cursor: pointer;
    aspect-ratio: 3/2; background: #1a1f1c;
  }
  .sk-card img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s ease; }
  .sk-card:hover img { transform: scale(1.05); }
  .sk-card-overlay {
    position: absolute; bottom: 0; left: 0; right: 0; padding: 3rem 1.25rem 1.25rem;
    background: linear-gradient(to top, rgba(0,0,0,0.85), transparent);
    opacity: 0; transition: opacity 0.3s;
  }
  .sk-card:hover .sk-card-overlay { opacity: 1; }
  .sk-card-title { font-family: 'Playfair Display', serif; font-size: 1.05rem; margin-bottom: 0.2rem; }
  .sk-card-loc { font-size: 0.8rem; color: #8a8474; }
  .sk-badge {
    position: absolute; top: 1rem; right: 1rem; padding: 0.3rem 0.75rem;
    background: rgba(91,140,62,0.9); border-radius: 100px; font-size: 0.65rem;
    font-weight: 600; text-transform: uppercase; letter-spacing: 1px;
  }
  .sk-play-icon {
    position: absolute; top: 50%; left: 50%; transform: translate(-50%,-50%);
    width: 56px; height: 56px; background: rgba(255,255,255,0.15); border-radius: 50%;
    display: flex; align-items: center; justify-content: center; backdrop-filter: blur(4px);
    pointer-events: none; border: 2px solid rgba(255,255,255,0.3);
  }

  /* FIELD GUIDE / DEX */
  .sk-dex {
    display: flex; align-items: center; justify-content: center; gap: 1.5rem;
    position: relative; min-height: 520px;
  }
  .sk-dex-viewport {
    position: relative; width: 320px; height: 480px; perspective: 900px;
  }
  .sk-dex-card {
    position: absolute; inset: 0; opacity: 0; pointer-events: none;
    transition: opacity 0.4s ease, transform 0.4s ease;
    transform: scale(0.9) translateX(40px);
  }
  .sk-dex-card.active {
    opacity: 1; pointer-events: auto; transform: scale(1) translateX(0);
  }
  .sk-dex-card.prev {
    opacity: 0; transform: scale(0.9) translateX(-40px);
  }
  .sk-dex-card-inner {
    width: 100%; height: 100%; position: relative;
    transition: transform 0.6s ease; transform-style: preserve-3d; cursor: pointer;
  }
  .sk-dex-card.flipped .sk-dex-card-inner { transform: rotateY(180deg); }
  .sk-dex-front, .sk-dex-back {
    position: absolute; inset: 0; backface-visibility: hidden; -webkit-backface-visibility: hidden;
    border-radius: 16px; padding: 1.5rem; display: flex; flex-direction: column;
    background: linear-gradient(145deg, #1a1f1c 0%, #141914 100%);
    border: 2px solid #2a302c; overflow: hidden;
  }
  .sk-dex-back { transform: rotateY(180deg); }

  /* Rarity border glow */
  .sk-dex-card[data-rarity="common"] .sk-dex-front,
  .sk-dex-card[data-rarity="common"] .sk-dex-back { border-color: #3d5c2e; }
  .sk-dex-card[data-rarity="uncommon"] .sk-dex-front,
  .sk-dex-card[data-rarity="uncommon"] .sk-dex-back { border-color: #5B8C3E; box-shadow: 0 0 20px rgba(91,140,62,0.15); }
  .sk-dex-card[data-rarity="rare"] .sk-dex-front,
  .sk-dex-card[data-rarity="rare"] .sk-dex-back {
    border-color: #C4813D; box-shadow: 0 0 25px rgba(196,129,61,0.25);
  }

  .sk-dex-number {
    font-family: 'Inter', monospace; font-size: 0.75rem; font-weight: 600;
    color: #5B8C3E; letter-spacing: 1px;
  }
  .sk-dex-rarity {
    position: absolute; top: 1.25rem; right: 1.5rem;
    font-size: 0.65rem; font-weight: 600; text-transform: uppercase; letter-spacing: 1.5px;
    padding: 0.2rem 0.6rem; border-radius: 100px;
  }
  [data-rarity="common"] .sk-dex-rarity { color: #5B8C3E; background: rgba(91,140,62,0.15); }
  [data-rarity="uncommon"] .sk-dex-rarity { color: #7DB356; background: rgba(91,140,62,0.2); }
  [data-rarity="rare"] .sk-dex-rarity { color: #C4813D; background: rgba(196,129,61,0.2); }

  .sk-dex-img-wrap {
    flex: 1; margin: 0.75rem 0; border-radius: 10px; overflow: hidden;
    background: #0f1210; min-height: 0;
  }
  .sk-dex-img-wrap img { width: 100%; height: 100%; object-fit: cover; }
  .sk-dex-name {
    font-family: 'Playfair Display', serif; font-size: 1.3rem; font-weight: 700;
    margin-bottom: 0.15rem;
  }
  .sk-dex-scientific {
    font-size: 0.8rem; color: #7DB356; font-style: italic; margin-bottom: 0.5rem;
  }
  .sk-dex-quick {
    display: flex; justify-content: space-between;
    font-size: 0.75rem; color: #8a8474; margin-bottom: 0.25rem;
  }
  .sk-dex-hint {
    font-size: 0.65rem; color: #555; text-align: center; margin-top: auto;
    letter-spacing: 0.5px; text-transform: uppercase;
  }

  /* Back face */
  .sk-dex-back .sk-dex-name { margin-top: 0.25rem; margin-bottom: 1rem; }
  .sk-dex-details { flex: 1; display: flex; flex-direction: column; gap: 0.6rem; }
  .sk-dex-row {
    display: flex; justify-content: space-between; align-items: baseline;
    font-size: 0.8rem; padding-bottom: 0.5rem; border-bottom: 1px solid #2a302c;
  }
  .sk-dex-row:last-child { border-bottom: none; }
  .sk-dex-label { color: #8a8474; font-size: 0.7rem; text-transform: uppercase; letter-spacing: 0.5px; }
  .sk-dex-warn { color: #C4813D; font-weight: 600; }

  /* Nav arrows */
  .sk-dex-nav {
    width: 48px; height: 48px; border-radius: 50%; border: 1px solid #2a302c;
    display: flex; align-items: center; justify-content: center;
    color: #8a8474; transition: all 0.2s; background: rgba(26,31,28,0.8);
    flex-shrink: 0;
  }
  .sk-dex-nav:hover { border-color: #5B8C3E; color: #7DB356; background: rgba(91,140,62,0.1); }

  .sk-dex-counter {
    text-align: center; margin-top: 1.5rem; font-size: 0.85rem; color: #8a8474;
    font-weight: 500; letter-spacing: 1px;
  }
  .sk-dex-counter span:first-child { color: #7DB356; font-weight: 700; }

  @media (max-width: 480px) {
    .sk-dex-viewport { width: 280px; height: 420px; }
    .sk-dex { gap: 0.75rem; min-height: 460px; }
    .sk-dex-nav { width: 40px; height: 40px; }
  }

  /* STATS */
  .sk-stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.25rem; margin-bottom: 4rem; }
  .sk-stat-card {
    background: #1a1f1c; border: 1px solid #2a302c; border-radius: 12px;
    padding: 2rem 1.5rem; text-align: center; transition: border-color 0.3s, transform 0.3s;
  }
  .sk-stat-card:hover { border-color: #3d5c2e; transform: translateY(-2px); }
  .sk-stat-icon { color: #5B8C3E; margin-bottom: 1rem; }
  .sk-stat-num { font-family: 'Playfair Display', serif; font-size: 2.75rem; font-weight: 700; margin-bottom: 0.25rem; }
  .sk-stat-label { color: #8a8474; font-size: 0.85rem; font-weight: 400; text-transform: uppercase; letter-spacing: 1px; }

  .sk-recent { max-width: 700px; margin: 0 auto; }
  .sk-recent h3 { font-size: 1.5rem; margin-bottom: 1.5rem; text-align: center; }
  .sk-sighting {
    display: flex; justify-content: space-between; align-items: center;
    padding: 1.1rem 0; border-bottom: 1px solid #2a302c;
  }
  .sk-sighting:last-child { border-bottom: none; }
  .sk-sighting-species { font-weight: 500; margin-bottom: 0.15rem; }
  .sk-sighting-loc { color: #8a8474; font-size: 0.85rem; }
  .sk-sighting-date { color: #5B8C3E; font-size: 0.85rem; white-space: nowrap; font-weight: 500; }

  /* MAP */
  .sk-map-chips { display: flex; gap: 0.6rem; justify-content: center; margin-bottom: 1.5rem; }
  .sk-chip {
    padding: 0.45rem 1.25rem; border-radius: 100px; border: 1px solid #2a302c;
    font-size: 0.8rem; font-weight: 500; transition: all 0.2s; background: transparent; cursor: pointer;
  }
  .sk-chip.active { background: rgba(91,140,62,0.15); border-color: #5B8C3E; color: #7DB356; }
  .sk-chip:hover { border-color: #5B8C3E; }

  .sk-map-wrap { max-width: 800px; margin: 0 auto; }
  .sk-svg-container { position: relative; width: 100%; }
  .sk-svg-container svg { width: 100%; height: auto; display: block; }
  .sk-svg-wrap { border-radius: 8px; overflow: hidden; background: rgba(255,255,255,0.02); }
  .sk-svg-wrap [data-state] {
    transform-box: fill-box;
    transform-origin: center;
    cursor: pointer;
    transition: fill 0.2s ease, stroke 0.2s ease, transform 0.25s ease, filter 0.25s ease;
  }
  .sk-map-loading { text-align: center; padding: 4rem 0; color: #555; font-size: 0.9rem; }

  .sk-map-legend {
    display: flex; align-items: center; justify-content: center; gap: 0.75rem;
    margin-top: 1.25rem; font-size: 0.75rem; color: #8a8474;
  }
  .sk-legend-gradient {
    width: 200px; height: 10px; border-radius: 5px;
    background: linear-gradient(to right, rgba(91,140,62,0.08), rgba(91,140,62,0.9));
  }

  .sk-state-detail {
    max-width: 500px; margin: 2rem auto 0; background: #1a1f1c; border: 1px solid #2a302c;
    border-radius: 12px; padding: 1.75rem 2rem; position: relative;
  }
  .sk-detail-close { position: absolute; top: 1rem; right: 1rem; opacity: 0.5; transition: opacity 0.2s; }
  .sk-detail-close:hover { opacity: 1; }
  .sk-detail-name { font-size: 1.4rem; margin-bottom: 1.25rem; }
  .sk-detail-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; margin-bottom: 1.25rem; }
  .sk-detail-item { text-align: center; }
  .sk-detail-val { display: block; font-family: 'Playfair Display', serif; font-size: 1.75rem; font-weight: 700; color: #7DB356; }
  .sk-detail-label { font-size: 0.75rem; color: #8a8474; text-transform: uppercase; letter-spacing: 1px; }
  .sk-detail-top { font-size: 0.9rem; color: #8a8474; }
  .sk-detail-top strong { color: #e8e4dc; font-style: italic; }

  .sk-tooltip {
    position: fixed; padding: 0.85rem 1.1rem; background: #1a1f1c;
    border: 1px solid #2a302c; border-left: 3px solid #5B8C3E;
    border-radius: 4px; font-size: 0.85rem; pointer-events: none; z-index: 200;
    opacity: 0; white-space: nowrap;
  }
  .sk-tooltip.visible { opacity: 1; animation: sk-tooltip-in 0.15s ease forwards; }
  @keyframes sk-tooltip-in { from { opacity: 0; transform: translateY(4px); } to { opacity: 1; transform: translateY(0); } }
  .sk-tooltip-name { font-weight: 700; font-size: 0.95rem; margin-bottom: 0.35rem; }
  .sk-tooltip-metrics { color: #7DB356; font-size: 0.8rem; margin-bottom: 0.15rem; }
  .sk-tooltip-species { color: #8a8474; font-size: 0.75rem; font-style: italic; }

  /* LIGHTBOX */
  .sk-lightbox {
    position: fixed; inset: 0; z-index: 1000; background: rgba(0,0,0,0.96);
    display: flex; align-items: center; justify-content: center;
    opacity: 0; pointer-events: none; transition: opacity 0.3s;
  }
  .sk-lightbox.open { opacity: 1; pointer-events: auto; }
  .sk-lb-close { position: absolute; top: 1.5rem; right: 1.5rem; opacity: 0.6; transition: opacity 0.2s; z-index: 1001; }
  .sk-lb-close:hover { opacity: 1; }
  .sk-lb-body { max-width: 90vw; max-height: 90vh; display: flex; flex-direction: column; align-items: center; }
  .sk-lb-img { max-width: 100%; max-height: 70vh; object-fit: contain; border-radius: 4px; }
  .sk-lb-info { margin-top: 1.5rem; text-align: center; }
  .sk-lb-title { font-size: 1.5rem; margin-bottom: 0.25rem; }
  .sk-lb-species { color: #7DB356; font-style: italic; font-size: 0.9rem; margin-bottom: 0.25rem; }
  .sk-lb-meta { color: #8a8474; font-size: 0.9rem; margin-bottom: 1.25rem; }
  .sk-lb-buy {
    display: inline-block; padding: 0.65rem 1.75rem; background: #C4813D; color: white;
    border-radius: 4px; font-weight: 500; font-size: 0.9rem; transition: background 0.2s;
  }
  .sk-lb-buy:hover { background: #a86d32; }
  .sk-lb-nav {
    position: absolute; top: 50%; transform: translateY(-50%); opacity: 0.4;
    transition: opacity 0.2s; padding: 1rem;
  }
  .sk-lb-nav:hover { opacity: 1; }
  .sk-lb-prev { left: 1rem; }
  .sk-lb-next { right: 1rem; }

  /* FOOTER */
  .sk-footer { padding: 3rem 0; border-top: 1px solid #2a302c; text-align: center; }
  .sk-footer-tag { font-family: 'Playfair Display', serif; font-size: 1.05rem; color: #8a8474; margin-bottom: 0.5rem; font-style: italic; }
  .sk-footer-copy { font-size: 0.8rem; color: #444; }

  /* ANIMATIONS */
  .sk-anim { opacity: 0; transform: translateY(24px); transition: opacity 0.7s ease, transform 0.7s ease; }
  .sk-anim.visible { opacity: 1; transform: translateY(0); }

  /* RESPONSIVE */
  @media (max-width: 1024px) {
    .sk-stats-grid { grid-template-columns: repeat(2, 1fr); }
    .sk-gallery-grid { grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); }
  }
  @media (max-width: 768px) {
    .sk-hero h1 { font-size: 3.2rem; }
    .sk-section-title { font-size: 2rem; }
    .sk-nav-links { display: none; flex-direction: column; position: absolute; top: 100%; left: 0; right: 0; background: rgba(15,18,16,0.98); padding: 1.5rem 2rem; gap: 1.25rem; backdrop-filter: blur(12px); }
    .sk-nav-links.open { display: flex; }
    .sk-nav-toggle { display: block; }
    .sk-sighting { flex-direction: column; align-items: flex-start; gap: 0.35rem; }
    .sk-section { padding: 4rem 0; }
    .sk-detail-grid { grid-template-columns: repeat(3, 1fr); gap: 0.75rem; }
    .sk-card-overlay { opacity: 1; }
  }
  @media (max-width: 480px) {
    .sk-hero h1 { font-size: 2.5rem; }
    .sk-hero-label { font-size: 0.65rem; letter-spacing: 3px; }
    .sk-stats-grid { grid-template-columns: 1fr 1fr; }
    .sk-gallery-grid { grid-template-columns: 1fr; }
    .sk-hero-buttons { flex-direction: column; align-items: center; }
    .sk-btn-primary, .sk-btn-outline { width: 100%; text-align: center; }
    .sk-lb-nav { display: none; }
    .sk-stat-num { font-size: 2rem; }
    .sk-container { padding: 0 1.25rem; }
  }
</style>

<script define:vars={{ galleryData: galleryItems, contactEmail: CONTACT_EMAIL, stateData: stateVisits }}>
  document.addEventListener('DOMContentLoaded', () => {
    // Nav scroll
    const nav = document.getElementById('sk-nav');
    window.addEventListener('scroll', () => {
      nav.classList.toggle('scrolled', window.scrollY > 50);
    }, { passive: true });

    // Mobile nav
    document.getElementById('sk-nav-toggle').addEventListener('click', () => {
      document.getElementById('sk-nav-links').classList.toggle('open');
    });
    document.querySelectorAll('.sk-nav-links a').forEach(a => {
      a.addEventListener('click', () => document.getElementById('sk-nav-links').classList.remove('open'));
    });

    // Gallery filters
    const filterBtns = document.querySelectorAll('.sk-filter-btn');
    const cards = document.querySelectorAll('.sk-card');
    filterBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        const f = btn.dataset.filter;
        filterBtns.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        cards.forEach(c => { c.style.display = (f === 'all' || c.dataset.category === f) ? '' : 'none'; });
      });
    });

    // Field Guide / Dex
    const dexCards = document.querySelectorAll('.sk-dex-card');
    const dexCurrent = document.getElementById('dex-current');
    let dexIdx = 0;

    function showDexCard(idx) {
      dexCards.forEach((card, i) => {
        card.classList.remove('active', 'prev');
        card.classList.remove('flipped');
        if (i === idx) card.classList.add('active');
        else if (i < idx) card.classList.add('prev');
      });
      dexIdx = idx;
      if (dexCurrent) dexCurrent.textContent = String(idx + 1);
    }

    document.getElementById('dex-prev')?.addEventListener('click', () => {
      showDexCard((dexIdx - 1 + dexCards.length) % dexCards.length);
    });
    document.getElementById('dex-next')?.addEventListener('click', () => {
      showDexCard((dexIdx + 1) % dexCards.length);
    });

    dexCards.forEach(card => {
      card.addEventListener('click', () => {
        if (card.classList.contains('active')) {
          card.classList.toggle('flipped');
        }
      });
    });

    showDexCard(0);

    // Keyboard nav for dex
    document.addEventListener('keydown', e => {
      const lbOpen = document.getElementById('sk-lightbox')?.classList.contains('open');
      if (lbOpen) return;
      const dexSection = document.getElementById('field-guide');
      if (!dexSection) return;
      const rect = dexSection.getBoundingClientRect();
      const visible = rect.top < window.innerHeight && rect.bottom > 0;
      if (!visible) return;
      if (e.key === 'ArrowLeft') { e.preventDefault(); showDexCard((dexIdx - 1 + dexCards.length) % dexCards.length); }
      if (e.key === 'ArrowRight') { e.preventDefault(); showDexCard((dexIdx + 1) % dexCards.length); }
    });

    // Lightbox
    const lb = document.getElementById('sk-lightbox');
    const lbImg = lb.querySelector('.sk-lb-img');
    const lbTitle = lb.querySelector('.sk-lb-title');
    const lbSpecies = lb.querySelector('.sk-lb-species');
    const lbMeta = lb.querySelector('.sk-lb-meta');
    const lbBuy = lb.querySelector('.sk-lb-buy');
    let currentIdx = 0;
    let filtered = galleryData;

    function showItem(idx) {
      const item = filtered[idx];
      if (!item) return;
      lbImg.src = item.src || item.thumb;
      lbImg.alt = item.title;
      lbTitle.textContent = item.title;
      lbSpecies.textContent = item.species || '';
      lbSpecies.style.display = item.species ? '' : 'none';
      const d = new Date(item.date + 'T00:00:00');
      lbMeta.textContent = item.location + ' \u2022 ' + d.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' });
      if (item.forSale) {
        lbBuy.style.display = 'inline-block';
        const subj = encodeURIComponent('Purchase Inquiry: ' + item.title);
        const body = encodeURIComponent('Hi Shakshuka,\n\nI\'m interested in purchasing a print of "' + item.title + '".\n\nPlease let me know about available sizes and pricing.\n\nThank you!');
        lbBuy.href = 'mailto:' + contactEmail + '?subject=' + subj + '&body=' + body;
      } else {
        lbBuy.style.display = 'none';
      }
    }

    function openLB(id) {
      const activeF = document.querySelector('.sk-filter-btn.active')?.dataset.filter || 'all';
      filtered = activeF === 'all' ? galleryData : galleryData.filter(i => i.category === activeF);
      currentIdx = filtered.findIndex(i => i.id === id);
      if (currentIdx < 0) currentIdx = 0;
      showItem(currentIdx);
      lb.classList.add('open');
      document.body.style.overflow = 'hidden';
    }

    function closeLB() {
      lb.classList.remove('open');
      document.body.style.overflow = '';
    }

    cards.forEach(c => c.addEventListener('click', () => openLB(Number(c.dataset.id))));
    lb.querySelector('.sk-lb-close').addEventListener('click', closeLB);
    lb.querySelector('.sk-lb-prev').addEventListener('click', () => { currentIdx = (currentIdx - 1 + filtered.length) % filtered.length; showItem(currentIdx); });
    lb.querySelector('.sk-lb-next').addEventListener('click', () => { currentIdx = (currentIdx + 1) % filtered.length; showItem(currentIdx); });
    lb.addEventListener('click', e => { if (e.target === lb) closeLB(); });
    document.addEventListener('keydown', e => {
      if (!lb.classList.contains('open')) return;
      if (e.key === 'Escape') closeLB();
      if (e.key === 'ArrowLeft') { currentIdx = (currentIdx - 1 + filtered.length) % filtered.length; showItem(currentIdx); }
      if (e.key === 'ArrowRight') { currentIdx = (currentIdx + 1) % filtered.length; showItem(currentIdx); }
    });

    // Stat count-up
    function animateCount(el, target) {
      const duration = 2000;
      const start = performance.now();
      function tick(now) {
        const p = Math.min((now - start) / duration, 1);
        const eased = 1 - Math.pow(1 - p, 3);
        el.textContent = Math.round(target * eased).toLocaleString();
        if (p < 1) requestAnimationFrame(tick);
      }
      requestAnimationFrame(tick);
    }

    // Scroll animations
    const observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          if (entry.target.classList.contains('sk-stat-num')) {
            animateCount(entry.target, parseInt(entry.target.dataset.target));
          }
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.15 });

    document.querySelectorAll('.sk-anim, .sk-stat-num').forEach(el => observer.observe(el));

    // ========== SVG HEATMAP ==========
    let activeMetric = 'visits';
    const svgContainer = document.getElementById('sk-svg-container');
    const tip = document.getElementById('sk-tooltip');
    const tipName = tip.querySelector('.sk-tooltip-name');
    const tipMetrics = tip.querySelector('.sk-tooltip-metrics');
    const tipSpecies = tip.querySelector('.sk-tooltip-species');
    const detailPanel = document.getElementById('sk-state-detail');
    const legendMin = document.querySelector('.sk-legend-min');
    const legendMax = document.querySelector('.sk-legend-max');

    // Fetch and inject SVG
    async function loadMap() {
      try {
        const res = await fetch('/images/shakshuka/us-states.svg');
        const svgText = await res.text();
        svgContainer.innerHTML = '<div class="sk-svg-wrap">' + svgText + '</div>';
        svgContainer.querySelectorAll('title').forEach(t => t.remove());
        svgContainer.querySelectorAll('.dccircle').forEach(el => el.remove());
        colorMap(activeMetric);
        attachMapListeners();
      } catch (e) {
        svgContainer.innerHTML = '<p style="text-align:center;color:#555;padding:3rem 0">Map could not be loaded.</p>';
      }
    }

    function getMetricValue(code, metric) {
      const d = stateData[code];
      if (!d) return 0;
      return d[metric] || 0;
    }

    function colorMap(metric) {
      const paths = svgContainer.querySelectorAll('[data-state]');
      const maxVal = Math.max(...Object.values(stateData).map(d => d[metric] || 0), 1);
      legendMin.textContent = '0';
      legendMax.textContent = maxVal.toLocaleString();

      paths.forEach(path => {
        const code = path.getAttribute('data-state');
        const val = getMetricValue(code, metric);
        if (val > 0) {
          const intensity = val / maxVal;
          const opacity = Math.max(intensity * 0.85, 0.12);
          path.style.fill = 'rgba(91, 140, 62, ' + opacity + ')';
        } else {
          path.style.fill = 'rgba(255, 255, 255, 0.04)';
        }
        path.style.stroke = '#1a1f1c';
        path.style.strokeWidth = '0.5';
      });
    }

    function attachMapListeners() {
      const paths = svgContainer.querySelectorAll('[data-state]');
      paths.forEach(path => {
        const code = path.getAttribute('data-state');
        const titleEl = path.querySelector('title');
        const name = titleEl ? titleEl.textContent : code;

        path.addEventListener('mouseenter', () => {
          const d = stateData[code];
          tipName.textContent = name;
          if (d) {
            tipMetrics.textContent = d.visits + ' visits \u00B7 ' + d.species + ' species \u00B7 ' + d.photos.toLocaleString() + ' photos';
            tipSpecies.textContent = 'Top: ' + d.topSpecies;
          } else {
            tipMetrics.textContent = 'Not yet visited';
            tipSpecies.textContent = '';
          }
          tip.classList.add('visible');

          const svg = svgContainer.querySelector('svg');
          if (!svg) return;

          // Calculate directional lift away from map center
          const bbox = path.getBBox();
          const cx = bbox.x + bbox.width / 2;
          const cy = bbox.y + bbox.height / 2;
          const mapCx = 959 / 2;
          const mapCy = 593 / 2;
          let dx = cx - mapCx;
          let dy = cy - mapCy;
          const dist = Math.sqrt(dx * dx + dy * dy) || 1;
          const liftDist = 10;
          dx = (dx / dist) * liftDist;
          dy = (dy / dist) * liftDist;

          // Convert SVG units to CSS pixels
          const svgRect = svg.getBoundingClientRect();
          const pxPerUnit = svgRect.width / 959;
          const cssDx = dx * pxPerUnit;
          const cssDy = dy * pxPerUnit;

          path.style.transform = 'translate(' + cssDx + 'px, ' + cssDy + 'px) scale(1.03)';
          path.style.filter = 'brightness(1.4) drop-shadow(0 0 8px rgba(91, 140, 62, 0.6))';
          path.style.stroke = '#5B8C3E';
          path.style.strokeWidth = '1.5';
        });

        path.addEventListener('mousemove', e => {
          let x = e.clientX + 16;
          let y = e.clientY + 16;
          if (x + tip.offsetWidth > window.innerWidth - 8)
            x = e.clientX - tip.offsetWidth - 16;
          if (y + tip.offsetHeight > window.innerHeight - 8)
            y = e.clientY - tip.offsetHeight - 16;
          tip.style.left = x + 'px';
          tip.style.top = y + 'px';
        });

        path.addEventListener('mouseleave', () => {
          tip.classList.remove('visible');
          path.style.transform = '';
          path.style.filter = '';
          path.style.stroke = '#1a1f1c';
          path.style.strokeWidth = '0.5';
        });

        path.addEventListener('click', () => {
          const d = stateData[code];
          if (!d) return;
          detailPanel.style.display = 'block';
          detailPanel.querySelector('.sk-detail-name').textContent = d.name;
          document.getElementById('sd-visits').textContent = d.visits;
          document.getElementById('sd-species').textContent = d.species;
          document.getElementById('sd-photos').textContent = d.photos.toLocaleString();
          detailPanel.querySelector('.sk-detail-top-name').textContent = d.topSpecies;
          detailPanel.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        });
      });
    }

    // Detail close
    document.getElementById('sk-detail-close').addEventListener('click', () => {
      detailPanel.style.display = 'none';
    });

    // Metric toggle chips
    document.querySelectorAll('.sk-chip').forEach(chip => {
      chip.addEventListener('click', () => {
        document.querySelectorAll('.sk-chip').forEach(c => c.classList.remove('active'));
        chip.classList.add('active');
        activeMetric = chip.dataset.metric;
        colorMap(activeMetric);
      });
    });

    loadMap();
  });
</script>
